import { StyleSheet } from 'react-native';

export const Global = StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: "row",
        backgroundColor: 'transparent',
        height: '100%'
    }
})